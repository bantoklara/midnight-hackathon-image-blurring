{
  "name": "truemask",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "contract",
    "api"
  ],
  "scripts": {
    "compile": "cd contract && npm run compact",
    "build": "cd contract && npm run build && cd ../api && npm run build"
  },
  "devDependencies": {
    "shx": "^0.4.0",
    "typescript": "^5.9.3"
  }
}
